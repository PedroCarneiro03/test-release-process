name: Deploy Pipeline (stub)
on:
  workflow_call:
    inputs:
      environment-name:
        required: true
        type: string
jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Would deploy pipeline to ${{ inputs.environment-name }}"
