name: Deploy CDC Data Worker (stub)
on:
  workflow_call:
    inputs:
      environment-name:
        required: true
        type: string
jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Would deploy cdc-data-worker to ${{ inputs.environment-name }}"
