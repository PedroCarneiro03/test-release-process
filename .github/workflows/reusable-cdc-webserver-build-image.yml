name: Build CDC Webserver (stub)
on:
  workflow_call:
    inputs:
      environment-name:
        required: true
        type: string
jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Would build cdc-webserver for ${{ inputs.environment-name }}"
