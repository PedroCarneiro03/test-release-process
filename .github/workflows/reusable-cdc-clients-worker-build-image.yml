name: Build CDC Clients Worker (stub)
on:
  workflow_call:
    inputs:
      environment-name:
        required: true
        type: string
jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Would build cdc-clients-worker for ${{ inputs.environment-name }}"
